import React, { Component } from 'react';
import Tour from '../pages/Tour';

// This is the same as the Tour page for now.
// Maintaining a separate page for future development.
export default function PromoPage() {
  return <Tour />;
}